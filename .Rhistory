install.packages(c("bbmle", "emdbook", "Hmisc", "plotrix"))
plot(cars)
require(data.table)
seed_pred = fread("data/seedpred.dat")
seed_pred = fread("data/seedpred.txt")
seed_pred = fread("data/seedpred.csv")
seed_pred
seed_pred = fread("data\seedpred.dat")
seed_pred = fread("data/seedpred.dat")
```{r setup, INCLUDE = false}
require(data.table)
knitr::opts_knit$get("root.dir")
seed_pred = fread("data/seedpred.dat")
seed_pred
getwd()
knitr::opts_knit$get("root.dir")
knitr::opts_knit$set(root.dir = rprojroot::find_rstudio_root_file())
require(data.table)
knitr::opts_knit$set(root.dir = rprojroot::find_rstudio_root_file())
getwd()
seed_pred = fread("data/seedpred.dat")
seed_pred
require(data.table)
require(emdbook)
knitr::opts_knit$set(root.dir = rprojroot::find_rstudio_root_file())
data("SeedPred")
SeedPred
class(SeedPred)
class(seed_pred)
summary(seed_pred)
summary(SeedPred)
summary(seed_pred)
summary(SeedPred)
getwd()
data("SeedPred")
SeedPred
class(SeedPred)
seed_pred = fread("data/seedpred.dat")
seed_pred
class(seed_pred)
summary(seed_pred)
summary(SeedPred)
summary(seed_pred)
seed_pred
summary(SeedPred)
read.table("data/seedpred.dat")
read.table("data/seedpred.dat", header = TRUE)
T = 5
T
T = TRUE
SeedPred = data.table(SeedPred)
summary(SeedPred)
SeedPred[, matplot(seeds, species)]
SeedPred[, matplot(seeds, lty = species)]
SeedPred[, matplot(seeds)]
SeedPred[, matlines(seeds)]
SeedPred[, matplot(seeds, pch = 1)]
SeedPred
SeedPred[, matplot(seeds, date, h = 1)]
SeedPred[, matplot(y = seeds, x = date, h = 1)]
SeedPred[, matplot(y = seeds, x = date, pch = 1)]
SeedPred[, by(seeds, species, mean)]
SeedPred[, by(seeds, species, mean, na.rm = TRUE)]
SeedPred[, by(seeds, INDICES = list(species, date), mean, na.rm = TRUE)]
seed_pred
SeedPred[, mean(seeds), by = list(species)]
SeedPred[, mean(seeds), by = list(species, date)]
SeedPred[, mean(seeds), by = list(species, date)][, matplot(x = date, y = seeds)]
SeedPred[, mean(seeds), by = list(species, date)]
SeedPred[, seeds = mean(seeds), by = list(species, date)]
SeedPred[, seeds = mean(seeds), by = list(species, date)]
SeedPred[, mean(seeds), by = list(species, date)]
SeedPred[, seeds:= mean(seeds), by = list(species, date)]
SeedPred[, .(mean(seeds)), by = list(species, date)]
SeedPred[, .(seeds = mean(seeds)), by = list(species, date)]
SeedPred[, .(seeds = mean(seeds)), by = list(species, date)][, matplot(x = date, y = seeds)]
SeedPred[, .(seeds = mean(seeds)), by = list(species, date)]
data("SeedPred")
seed_pred = data.table(SeedPred)
seed_pred[, .(seeds = mean(seeds)), by = list(species, date)]
seed_pred[, .(seeds = mean(seeds)), by = list(species, date)]
seed_pred[, .(seeds = mean(seeds)), by = list(species, date)][, matplot(x = date, y = seeds)]
seed_pred[, .(seeds = mean(seeds)), by = list(species, date)][, matplot(x = date, y = seeds, pch = as.integer(species))]
seed_pred[, .(seeds = mean(seeds)), by = list(species, date)][, matplot(x = date, y = seeds, pch = as.integer(factor(species)))]
seed_pred[, species]
seed_pred[, factor(species)]
seed_pred[, as.int(factor(species))]
seed_pred[, as.numeric(factor(species))]
seed_pred[, as.integer(factor(species))]
seed_pred[, .(seeds = mean(seeds), as.integer(species)), by = list(species, date)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, pch]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch))]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
---
title: "R Notebook"
output: html
---
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matpoints(x = date, y = seeds, pch = pch)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matpoints(x = date, y = seeds, pch = pch)]
getwd()
data("SeedPred")
SeedPred
class(SeedPred)
seed_pred = fread("data/seedpred.dat")
seed_pred = data.table(SeedPred)
class(seed_pred)
read.table("data/seedpred.dat", header = TRUE)
summary(seed_pred)
summary(SeedPred)
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, pch]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matpoints(x = date, y = seeds, pch = pch)]
seed_pred[, as.integer(factor(species))]
seed_pred
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
getwd()
data("SeedPred")
SeedPred
class(SeedPred)
seed_pred = fread("data/seedpred.dat")
seed_pred = data.table(SeedPred)
class(seed_pred)
read.table("data/seedpred.dat", header = TRUE)
summary(seed_pred)
summary(SeedPred)
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, pch]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
# seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matpoints(x = date, y = seeds, pch = pch)]
---
title: "Bolker 2008 Chapter 2"
output: html_document
---
```{r setup, INCLUDE = false}
require(data.table)
require(emdbook)
knitr::opts_knit$set(root.dir = rprojroot::find_rstudio_root_file())
getwd()
data("SeedPred")
SeedPred
class(SeedPred)
seed_pred = fread("data/seedpred.dat")
seed_pred = fread("data/seedpred.dat")
seed_pred = data.table(SeedPred)
class(seed_pred)
summary(seed_pred)
summary(SeedPred)
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, pch]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds)]
seed_pred[, matplot(seeds, data)]
seed_pred[, matplot(seeds, date)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds)]
seed_pred[, plot(seeds)]
plot(seed_pred$seeds)
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = pch)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = 2)]
seed_pred[, .(seeds = mean(seeds), species, pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds)]
seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)]
sp1 = seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)][, matplot(x = date, y = seeds, pch = 2)]
sp1[, matplot(x = date, y = seeds)]
sp1
sp1 = seed_pred[, .(seeds = mean(seeds), pch = as.integer(species)), by = list(species, date)]
sp1
sp1[, date(date)]
sp1
sp[, matplot(x = date, y = seeds, pch = 2)]
sp1[, matplot(x = date, y = seeds, pch = 2)]
sp1[, matplot(x = date, y = seeds, pch = pch)]
sp1[species == "spd"]
sp1[species == "abz"]
sp1[species == "cd"]
sp1 = seed_pred[, .(seeds = mean(seeds), ch = as.integer(species)), by = list(species, date)]
sp1
sp1[, matplot(x = date, y = seeds, pch = ch)]
sp1
require(data.table)
getwd()
data("SeedPred")
SeedPred
require(emdbook)
require(data.table)
require(emdbook)
knitr::opts_knit$set(root.dir = rprojroot::find_rstudio_root_file())
getwd()
data("SeedPred")
SeedPred
knitr::opts_chunk$set(echo = TRUE)
summary(cars)
sp1 = seed_pred[, .(seeds = mean(seeds), ch = as.integer(species)), by = list(species, date)]
data("SeedPred")
SeedPred
class(SeedPred)
seed_pred = fread("data/seedpred.dat")
seed_pred = data.table(SeedPred)
class(seed_pred)
read.table("data/seedpred.dat", header = TRUE)
summary(seed_pred)
summary(SeedPred)
sp1 = seed_pred[, .(seeds = mean(seeds), ch = as.integer(species)), by = list(species, date)]
sp1
sp1[, matplot(x = date, y = seeds, pch = 2)]
sp1[, matplot(x = date, y = seeds, pch = ch)]
sp1[, matlines(x = date, y = seeds, pch = ch)]
sp1[, {matplot(x = date, y = seeds, pch = ch); matlines(x = date, y = seeds, pch = ch)}]
sp1
melt(sp1, species)
melt(sp1, c("species")
melt(sp1, c("species"))
melt(sp1, c("species"))
melt(sp1, measure.vars =  c("species"))
sp1
melt(sp1, measure.vars =  c("species"))
sp1
sp1[, matplot(x = date, y = seeds, type = "n")]
sp1[, matplot(x = date, y = seeds, type = "n", axis.Date(1, date))]
sp1[, matplot(x = date, y = seeds, type = "n", axes = FALSE)]
sp1[, axis.Date(1, date)]
sp1[,
{
matplot(x = date, y = seeds, type = "n", axes = FALSE);
axis.Date(1, date)
}
]
